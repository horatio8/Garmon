// Public site configuration. Safe to commit — only contains the Supabase
// project URL and anon (public) key. Service-role keys NEVER go here.
//
// To enable the CMS, paste your Supabase project URL and anon key below
// (Settings -> API in the Supabase dashboard). Until both are filled, the
// site falls back to baked-in defaults and the admin panel shows a setup
// screen.
window.CMS_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
